# Career Log Portfolio

React, Vite, TypeScript 기반의 커리어 로그형 포트폴리오입니다.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Deploy

GitHub Pages 배포를 위해 `.github/workflows/deploy.yml`을 포함했습니다.

저장소 Settings에서 Pages source를 `GitHub Actions`로 설정한 뒤 `main` 브랜치에 push하면 배포됩니다.
